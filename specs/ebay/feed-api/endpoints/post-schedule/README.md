# POST /schedule

createSchedule

This method creates a schedule, which is a subscription to the specified schedule template.

## Parameters

_None_

## Request Body

```
{ "feedType": "string", "preferredTriggerHour": "11Z", "scheduleName": "My Schedule", "scheduleTemplateId": "string" }
```

## Response Example

```
{}
```

Tags: schedule
