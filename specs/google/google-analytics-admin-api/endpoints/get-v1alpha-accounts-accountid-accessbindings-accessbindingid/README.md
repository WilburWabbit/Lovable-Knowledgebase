# GET /v1alpha/accounts/{accountId}/accessBindings/{accessBindingId}

Gets information about an access binding.

Gets information about an access binding. Original Google API path template: `/v1alpha/{name=accounts/*/accessBindings/*}`

## Parameters

- **accountId** (string) *required* — Account identifier.
- **accessBindingId** (string) *required* — Access binding identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.accounts.accessBindings
