# POST /v1alpha/accounts/{accountId}/accessBindings

Creates an access binding on an account or property.

Creates an access binding on an account or property. Original Google API path template: `/v1alpha/{parent=accounts/*}/accessBindings`

## Parameters

- **accountId** (string) *required* — Account identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.accounts.accessBindings
