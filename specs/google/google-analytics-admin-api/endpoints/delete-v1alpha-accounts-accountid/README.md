# DELETE /v1alpha/accounts/{accountId}

Marks target Account as soft-deleted (ie: "trashed") and returns it.

Marks target Account as soft-deleted (ie: "trashed") and returns it. Original Google API path template: `/v1alpha/{name=accounts/*}`

## Parameters

- **accountId** (string) *required* — Account identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.accounts
