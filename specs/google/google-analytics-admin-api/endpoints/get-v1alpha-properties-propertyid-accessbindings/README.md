# GET /v1alpha/properties/{propertyId}/accessBindings

Lists all access bindings on an account or property.

Lists all access bindings on an account or property. Original Google API path template: `/v1alpha/{parent=properties/*}/accessBindings`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties.accessBindings
