# DELETE /v1beta/properties/{propertyId}

Marks target Property as soft-deleted (ie: "trashed") and returns it.

Marks target Property as soft-deleted (ie: "trashed") and returns it. Original Google API path template: `/v1beta/{name=properties/*}`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1beta.properties
