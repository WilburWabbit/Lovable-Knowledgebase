# POST /v1alpha/properties/{propertyId}/accessBindings

Creates an access binding on an account or property.

Creates an access binding on an account or property. Original Google API path template: `/v1alpha/{parent=properties/*}/accessBindings`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.properties.accessBindings
