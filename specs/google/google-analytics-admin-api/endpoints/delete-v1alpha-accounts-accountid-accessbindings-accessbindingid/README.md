# DELETE /v1alpha/accounts/{accountId}/accessBindings/{accessBindingId}

Deletes an access binding on an account or property.

Deletes an access binding on an account or property. Original Google API path template: `/v1alpha/{name=accounts/*/accessBindings/*}`

## Parameters

- **accountId** (string) *required* — Account identifier.
- **accessBindingId** (string) *required* — Access binding identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.accounts.accessBindings
