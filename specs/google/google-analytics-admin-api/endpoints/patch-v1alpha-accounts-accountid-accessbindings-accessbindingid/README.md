# PATCH /v1alpha/accounts/{accountId}/accessBindings/{accessBindingId}

Updates an access binding on an account or property.

Updates an access binding on an account or property. Original Google API path template: `/v1alpha/{accessBinding.name=accounts/*/accessBindings/*}`

## Parameters

- **accountId** (string) *required* — Account identifier.
- **accessBindingId** (string) *required* — Access binding identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.accounts.accessBindings
