# DELETE /v1beta/accounts/{accountId}

Marks target Account as soft-deleted (ie: "trashed") and returns it.

Marks target Account as soft-deleted (ie: "trashed") and returns it. Original Google API path template: `/v1beta/{name=accounts/*}`

## Parameters

- **accountId** (string) *required* — Account identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1beta.accounts
