# POST /v1alpha/properties/{propertyId}/audienceLists

Creates an audience list for later retrieval.

Creates an audience list for later retrieval. Original Google API path template: `/v1alpha/{parent=properties/*}/audienceLists`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.properties.audienceLists
