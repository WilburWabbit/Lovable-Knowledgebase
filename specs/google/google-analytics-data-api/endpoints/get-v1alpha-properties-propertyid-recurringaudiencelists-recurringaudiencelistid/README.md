# GET /v1alpha/properties/{propertyId}/recurringAudienceLists/{recurringAudienceListId}

Gets configuration metadata about a specific recurring audience list.

Gets configuration metadata about a specific recurring audience list. Original Google API path template: `/v1alpha/{name=properties/*/recurringAudienceLists/*}`

## Parameters

- **propertyId** (string) *required* — Property identifier.
- **recurringAudienceListId** (string) *required* — Recurring audience list identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties.recurringAudienceLists
