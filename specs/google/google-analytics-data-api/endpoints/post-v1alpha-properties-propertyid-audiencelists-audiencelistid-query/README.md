# POST /v1alpha/properties/{propertyId}/audienceLists/{audienceListId}:query

Retrieves an audience list of users.

Retrieves an audience list of users. Original Google API path template: `/v1alpha/{name=properties/*/audienceLists/*}:query`

## Parameters

- **propertyId** (string) *required* — Property identifier.
- **audienceListId** (string) *required* — Audience list identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.properties.audienceLists
