# GET /v1alpha/properties/{propertyId}/propertyQuotasSnapshot

Get all property quotas organized by quota category for a given property.

Get all property quotas organized by quota category for a given property. Original Google API path template: `/v1alpha/{name=properties/*/propertyQuotasSnapshot}`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties
