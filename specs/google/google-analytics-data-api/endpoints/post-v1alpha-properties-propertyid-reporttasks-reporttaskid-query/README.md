# POST /v1alpha/properties/{propertyId}/reportTasks/{reportTaskId}:query

Retrieves a report task's content.

Retrieves a report task's content. Original Google API path template: `/v1alpha/{name=properties/*/reportTasks/*}:query`

## Parameters

- **propertyId** (string) *required* — Property identifier.
- **reportTaskId** (string) *required* — Report task identifier.

## Request Body

```
{}
```

## Response Example

```
{}
```

Tags: v1alpha.properties.reportTasks
