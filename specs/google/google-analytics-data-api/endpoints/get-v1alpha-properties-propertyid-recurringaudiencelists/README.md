# GET /v1alpha/properties/{propertyId}/recurringAudienceLists

Lists all recurring audience lists for a property.

Lists all recurring audience lists for a property. Original Google API path template: `/v1alpha/{parent=properties/*}/recurringAudienceLists`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties.recurringAudienceLists
