# GET /v1alpha/properties/{propertyId}/audienceLists

Lists all audience lists for a property.

Lists all audience lists for a property. Original Google API path template: `/v1alpha/{parent=properties/*}/audienceLists`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null,response_example:
```

## Response Example

```
{}
```

Tags: v1alpha.properties.audienceLists
