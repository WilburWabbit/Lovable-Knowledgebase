# GET /v1beta/properties/{propertyId}/metadata

Returns metadata for dimensions and metrics available in reporting methods.

Returns metadata for dimensions and metrics available in reporting methods. Original Google API path template: `/v1beta/{name=properties/*/metadata}`

## Parameters

- **propertyId** (string) *required* — Property identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1beta.properties
