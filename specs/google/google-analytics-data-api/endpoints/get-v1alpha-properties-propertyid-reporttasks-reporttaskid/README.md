# GET /v1alpha/properties/{propertyId}/reportTasks/{reportTaskId}

Gets report metadata about a specific report task.

Gets report metadata about a specific report task. Original Google API path template: `/v1alpha/{name=properties/*/reportTasks/*}`

## Parameters

- **propertyId** (string) *required* — Property identifier.
- **reportTaskId** (string) *required* — Report task identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties.reportTasks
