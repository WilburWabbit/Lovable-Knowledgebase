# GET /v1alpha/properties/{propertyId}/audienceLists/{audienceListId}

Gets configuration metadata about a specific audience list.

Gets configuration metadata about a specific audience list. Original Google API path template: `/v1alpha/{name=properties/*/audienceLists/*}`

## Parameters

- **propertyId** (string) *required* — Property identifier.
- **audienceListId** (string) *required* — Audience list identifier.

## Request Body

```
null
```

## Response Example

```
{}
```

Tags: v1alpha.properties.audienceLists
