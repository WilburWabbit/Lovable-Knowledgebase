# POST /tagmanager/v2/accounts/{accountId}/containers/{containerId}:combine

Combines Containers.

Combines Containers.

## Parameters

- **accountId** (string) *required* — Google Tag Manager account ID.
- **containerId** (string) *required* — Google Tag Manager container ID.

## Request Body

```
{}
```

## Response Example

```
{"container": {"accountId": "string", "containerId": "string"}}
```

Tags: v2.accounts.containers
